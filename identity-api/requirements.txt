fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic[email]==2.10.3
pydantic-settings==2.6.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
redis==5.2.1
# Outbound HTTP to approval-api (re-sync in-flight approvals after a role
# change) — identity had no HTTP client before; pinned to the same version the
# other services use.
httpx==0.28.1
aiosmtplib==3.0.2
alembic==1.14.0

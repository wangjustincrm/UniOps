# 会议室预订系统 PRD

## 1. 文档信息

- 产品名称：企业内部会议室预订系统
- 文档版本：V1.0
- 文档日期：2026-07-07
- 文档状态：初稿
- 目标形态：企业内部 Web 系统

## 2. 产品背景

随着企业办公规模扩大，会议室资源逐渐成为高频共享资源。当前线下登记、口头协调或分散在 Outlook 邮件中的会议室使用方式，容易出现以下问题：

- 会议室空闲信息不透明，员工难以快速找到合适会议室
- 会议时间冲突频发，存在重复预订或临时占用
- 会议室设备信息分散，无法按容量、位置、设施快速筛选
- 会议通知和日历事件依赖人工操作，容易遗漏参会人同步
- 会议结束未及时释放会议室，导致资源利用率偏低

因此需要建设一套企业内部统一的会议室预订系统，与公司组织架构、企业邮件系统和 Outlook Classic Desktop 的会议邀请能力联动，提升会议室使用效率和员工预订体验。

## 3. 产品目标

### 3.1 业务目标

- 建立统一的会议室资源配置和预订入口
- 让员工可以在 1 分钟内完成会议室查询与预订
- 降低会议室冲突和重复占用
- 打通预订、邮件通知、会议邀请和日历事项触发的自动化流程
- 提升会议室利用率与会议管理透明度

### 3.2 用户目标

- 快速查看会议室当前状态和未来空闲时间
- 按时间、地点、容量、设备筛选可用会议室
- 预订成功后自动通知参会人，并在 Outlook Classic Desktop 中触发会议邀请/日历事项
- 能方便地改期、取消会议，减少手工沟通成本

### 3.3 成功指标

- 会议室预订成功率 >= 95%
- 重复预订冲突率 <= 1%
- 用户完成一次预订的平均时长 <= 60 秒
- 邮件投递与日历邀请生成成功率 >= 99%
- 会议室利用率统计可覆盖 100% 已配置会议室

## 4. 产品范围

### 4.1 本期范围

- 企业内部员工通过 Web 端进行会议室查询与预订
- 管理员配置会议室基础信息、可预订规则和状态
- 系统展示会议室当前状态与时间段占用情况
- 系统根据用户选择的时间智能提示可预订会议室和可替代时间
- 预订成功后自动向参会人发送邮件通知
- 预订成功后自动发送 Outlook 可识别的会议邀请并触发日历事项
- 支持预订变更、取消、状态回写和通知同步

### 4.2 暂不纳入本期

- 移动 App 原生端
- Outlook 插件内直接发起预订
- 门禁、电子门牌、IoT 占用传感器联动
- 会议签到硬件和二维码签到
- 跨园区访客接待流程

## 5. 目标用户与角色

### 5.1 普通员工

- 查询会议室状态
- 发起会议室预订
- 邀请参会人
- 修改、取消自己创建的预订

### 5.2 行政/会议室管理员

- 新增、编辑、停用会议室
- 配置会议室开放时间、设备、位置、预订规则
- 查看预订记录和使用统计
- 处理异常预订、黑名单、手动释放会议室

### 5.3 IT/系统管理员

- 配置组织账户、SSO、邮件服务、IMAP/SMTP 和 Outlook 客户端兼容策略
- 维护权限、日志、接口配置和告警策略

## 6. 核心业务假设

- 系统为企业内部系统，默认接入企业统一身份认证
- 默认使用公司员工通讯录作为参会人选择来源
- 默认预订策略为“自动通过”：只要会议室在所选时间段空闲，提交后立即生效
- 企业邮箱基于 IMAP Server，员工使用 Outlook Classic Desktop App 收发邮件
- 系统通过 SMTP 或企业邮件网关发送标准会议邀请邮件
- 会议室预订成功后，系统自动向参会人发送邮件，并通过 iCalendar/iTIP/iMIP 兼容格式触发 Outlook 日历事项
- Web 端为首期唯一使用入口

## 7. 用户场景

### 7.1 快速预订

员工需要在今天下午召开项目会，进入系统后输入时间、参会人数和设备需求，系统返回符合条件的空闲会议室，员工选择后立即完成预订，并自动通知参会人。

### 7.2 冲突替代

员工选择的会议室在目标时间已被占用，系统自动推荐该会议室的最近空闲时段，或推荐同楼层、同容量的替代会议室。

### 7.3 变更同步

会议时间调整后，会议发起人修改预订，系统同步更新会议室占用状态、邮件通知和 Outlook 会议邀请。

### 7.4 管理配置

管理员为新装修楼层批量录入会议室，配置位置、楼层、容量、设备、开放时段和禁用规则，使员工可按条件进行筛选和预订。

## 8. 产品流程概览

1. 员工登录系统
2. 选择会议日期、开始时间、结束时间、参会人数、会议主题
3. 系统查询可用会议室并展示空闲状态
4. 用户可按楼层、区域、容量、设备筛选结果
5. 用户选择会议室并添加参会人
6. 系统校验时间冲突、权限和预订规则
7. 校验通过后自动生成预订记录
8. 系统更新会议室状态和时间占用
9. 系统发送邮件通知参会人
10. 系统生成并发送 Outlook 可识别的会议邀请
11. 会议取消或修改时，系统同步发送更新或取消邀请

## 9. 功能需求

### 9.1 会议室配置管理

#### 9.1.1 功能目标

支持管理员统一维护企业会议室资源，为预订、状态展示和智能推荐提供基础数据。

#### 9.1.2 功能说明

- 支持新增、编辑、停用会议室
- 支持配置以下字段：
  - 会议室名称
  - 会议室编码
  - 所属园区 / 楼栋 / 楼层 / 区域
  - 容纳人数
  - 设备信息：电视、投影、白板、视频会议设备、电话会议设备等
  - 会议室类型：普通会议室、培训室、董事会会议室、多功能厅等
  - 可预订开放时间
  - 可预订日期范围
  - 默认状态：可用、停用、维修中
  - 负责人或管理部门
  - 备注说明
- 支持上传会议室图片
- 支持批量导入会议室配置
- 支持按楼层、区域、状态筛选管理会议室

#### 9.1.3 业务规则

- 停用或维修中的会议室不可被预订
- 会议室容量必须为正整数
- 同一组织内会议室编码唯一
- 修改会议室基础信息不影响历史预订记录，但会影响后续查询展示

### 9.2 会议室查询与预订

#### 9.2.1 功能目标

支持员工快速完成会议室检索、选择和预订，并在空闲条件下自动通过。

#### 9.2.2 功能说明

- 支持按日期、开始时间、结束时间查询空闲会议室
- 支持按以下条件筛选：
  - 园区 / 楼栋 / 楼层 / 区域
  - 会议室容量
  - 设备类型
  - 会议室类型
  - 当前状态
- 支持填写预订信息：
  - 会议主题
  - 会议说明
  - 发起人
  - 参会人
  - 是否需要视频会议设备
  - 是否为周期性会议
- 支持查看会议室详情页，包括位置、设备、容量、当天日程
- 支持一键提交预订

#### 9.2.3 业务规则

- 只要会议室在选定时间段内空闲，则自动预订成功
- 同一会议室同一时间段不可重复预订
- 结束时间必须晚于开始时间
- 默认单次会议最短 15 分钟，最长 4 小时
- 默认允许提前 30 天预订
- 周期性会议仅支持固定规则生成，不可与已存在预订冲突
- 创建人只能修改或取消自己发起的预订，管理员可管理全部预订

### 9.3 会议室状态展示

#### 9.3.1 功能目标

让员工清楚知道会议室当前是否可用，以及未来时间段的占用情况。

#### 9.3.2 功能说明

- 支持在列表页展示会议室实时状态：
  - 空闲
  - 使用中
  - 即将开始
  - 已预订
  - 停用
  - 维修中
- 支持展示会议室当天时间轴
- 支持查看未来 7 天预订情况
- 支持在会议室详情页显示下一场会议开始时间
- 支持根据当前时间动态刷新状态

#### 9.3.3 状态定义

- 空闲：当前时间段内无占用，可立即预订
- 使用中：当前处于预订生效时间内
- 即将开始：距离下一场会议开始小于等于 15 分钟
- 已预订：未来某时间段已被预约，但当前未开始
- 停用：管理员手动关闭使用
- 维修中：不可预订，且对员工展示不可用原因

### 9.4 Available 时间提示

#### 9.4.1 功能目标

在会议室无空闲或冲突时，帮助用户快速找到可用时间或替代资源。

#### 9.4.2 功能说明

- 当目标会议室冲突时，系统提示该会议室最近可用时间段
- 当查询条件下无满足条件的会议室时，系统推荐：
  - 相同时间段的其他可用会议室
  - 同一会议室的相邻可用时间段
  - 调整开始时间或结束时间后的可用选项
- 支持基于参会人数和设备需求进行推荐排序
- 支持在提交前实时提示冲突和替代方案

#### 9.4.3 推荐规则

- 优先推荐同楼层、同容量区间的会议室
- 其次推荐相邻楼层或同区域会议室
- 若无完全匹配会议室，优先满足时间，再满足容量，再满足设备
- 推荐时间段默认以 15 分钟粒度进行计算

### 9.5 邮件通知

#### 9.5.1 功能目标

在预订和变更发生后，自动通知相关人员，减少线下确认成本。

#### 9.5.2 功能说明

- 预订成功后，系统自动向以下对象发送邮件：
  - 会议发起人
  - 全部参会人
  - 会议室管理员（可配置是否接收）
- 邮件内容包括：
  - 会议主题
  - 会议时间
  - 会议室名称和位置
  - 参会人列表
  - 变更或取消说明
  - 会议邀请附件或 `text/calendar` 邀请内容
- 支持以下邮件触发场景：
  - 创建预订成功
  - 修改预订成功
  - 取消预订成功
  - 系统同步失败告警

#### 9.5.3 业务规则

- 同一事件只允许发送一次正式通知，失败后按重试机制补发
- 取消会议时需向原通知对象发送取消通知
- 会议邀请邮件需兼容 Outlook Classic Desktop 对 `text/calendar` 和 `.ics` 附件的识别
- 邮件发送失败需记录日志并触发系统告警

### 9.6 Outlook 日历邀请联动（基于 IMAP 邮件环境）

#### 9.6.1 功能目标

在无 Exchange 能力的前提下，通过标准会议邀请邮件让 Outlook Classic Desktop 能识别、创建、更新和取消会议事项，确保会议安排和会议室资源一致。

#### 9.6.2 功能说明

- 预订成功后，系统自动生成标准 iCalendar `VEVENT` 数据，并通过邮件发送会议邀请
- 修改预订后，系统使用相同日历 UID 发送更新邀请
- 取消预订后，系统使用相同日历 UID 发送取消邀请
- Outlook 邀请需包含：
  - 会议主题
  - 开始时间与结束时间
  - 会议室信息
  - 会议说明
  - 参会人
- 支持记录日历 UID、邀请版本号、发送批次号，便于后续更新和取消
- 支持邀请邮件中同时包含人类可读正文和机器可识别的日历内容
- 支持周期性会议生成对应的周期性日历邀请

#### 9.6.3 集成要求

- 支持企业现有 IMAP 邮件环境
- 系统发送能力依赖 SMTP 或企业邮件网关，而非 Exchange / Microsoft Graph API
- 会议邀请格式需兼容 iCalendar（RFC 5545）、iTIP（RFC 5546）以及基于邮件传输的 iMIP（RFC 6047）
- 需支持 `METHOD:REQUEST`、更新版本控制以及 `METHOD:CANCEL`
- 需记录邀请生成日志、邮件投递日志、失败原因和补偿结果

#### 9.6.4 约束说明

- 由于企业邮箱不使用 Exchange，系统默认不具备服务端直接写入 Outlook 日历的能力
- V1 通过“标准会议邀请邮件”触发 Outlook 客户端侧的日历处理流程，而非通过 Exchange 事件 API 直接创建事件
- V1 默认不做参会人“接受/拒绝/暂定”状态的自动回写；如需支持，后续需增加回复邮件解析或独立日历平台集成

### 9.7 预订变更与取消

#### 9.7.1 功能说明

- 发起人支持修改会议时间、会议室、参会人、会议主题和说明
- 发起人支持取消未开始的会议
- 管理员支持强制取消异常占用
- 系统需要在变更后同步更新：
  - 会议室占用状态
  - 邮件通知
  - Outlook 会议邀请

#### 9.7.2 业务规则

- 已开始的会议默认不允许修改时间，可由管理员特殊处理
- 取消后原时间段立即释放
- 修改预订时必须复用原日历 UID，并增加邀请版本号，避免 Outlook 生成重复事项
- 取消或修改操作必须保留审计日志

### 9.8 权限与账号

#### 9.8.1 功能说明

- 支持企业统一登录
- 支持角色权限控制
- 支持员工从企业通讯录中搜索参会人
- 支持管理员查看全局预订记录和配置项

#### 9.8.2 权限模型

- 普通员工：查询、预订、修改本人会议、取消本人会议
- 会议室管理员：管理会议室、查看全部预订、处理异常
- 系统管理员：管理系统配置、接口配置、权限配置、日志

### 9.9 运营后台与统计

#### 9.9.1 功能说明

- 支持查看会议室使用率
- 支持查看会议室预订次数、取消次数、冲突次数
- 支持按楼层、部门、时间范围统计
- 支持导出预订记录和使用报表

#### 9.9.2 核心指标

- 会议室利用率
- 平均预订时长
- 热门会议室排行
- 邮件发送成功率
- 会议邀请生成成功率

## 10. 关键业务规则

- 预订模式：自动通过
- 时间冲突校验必须在提交时实时执行
- 会议室状态刷新延迟不高于 1 分钟
- 默认预订时间粒度为 15 分钟
- 默认开放时间为工作日 08:00-20:00，管理员可配置
- 会议开始前 15 分钟内状态显示为“即将开始”
- 被停用或维修的会议室不参与推荐和预订
- 周期性会议每次生成独立占用记录和同步事件
- 日历邀请必须使用稳定的日历 UID 和版本号进行更新、取消
- 系统生成的邀请邮件必须同时包含可读正文与标准日历内容
- 参会人接受/拒绝状态回写不属于 V1 强制范围
- 系统必须保留完整操作日志、通知日志和同步日志

## 11. 异常场景与处理

### 11.1 预订提交时发生冲突

- 提示用户当前会议室已被占用
- 展示替代会议室与推荐时间段
- 不生成预订记录

### 11.2 邮件发送失败

- 预订仍然成功
- 系统记录失败日志并自动重试
- 重试失败后通知管理员处理

### 11.3 Outlook 会议邀请生成或发送失败

- 预订记录先落库成功
- 系统将同步任务标记为待补偿
- 管理后台显示同步异常状态
- 成功补偿后回写日历 UID、版本号和发送状态

### 11.4 会议室被管理员停用

- 对未来未开始的预订进行影响分析
- 由系统通知发起人重新选择会议室
- 管理员可手动迁移或取消相关预订

## 12. 非功能需求

### 12.1 性能要求

- 常规查询接口响应时间 <= 2 秒
- 预订提交接口响应时间 <= 3 秒
- 支持至少 1000 名员工并发查询
- 支持至少 200 个会议室的状态实时展示

### 12.2 可用性要求

- 系统可用性 >= 99.9%
- 邮件和日历同步服务需支持失败重试和补偿机制
- 关键操作需具备审计日志

### 12.3 安全要求

- 接入企业 SSO 和权限体系
- 敏感接口需鉴权和操作审计
- 员工只能访问授权范围内的数据
- 系统日志需符合企业安全规范

### 12.4 兼容性要求

- 支持企业常用桌面浏览器
- 兼容主流分辨率下的 Web 使用场景

## 13. 数据对象概览

### 13.1 会议室

- 会议室 ID
- 名称
- 编码
- 位置
- 容量
- 设备信息
- 状态
- 可用时间配置

### 13.2 预订记录

- 预订 ID
- 会议主题
- 发起人
- 参会人
- 开始时间
- 结束时间
- 会议室 ID
- 预订状态
- 日历 UID
- 邀请版本号
- 邮件发送状态
- 创建时间
- 修改时间

### 13.3 通知记录

- 通知 ID
- 通知类型
- 接收人
- 发送时间
- 发送状态
- 失败原因

## 14. 里程碑建议

### 14.1 第一阶段：MVP

- 会议室配置管理
- 基础查询与预订
- 状态展示
- Available 时间提示
- 邮件通知
- Outlook 兼容会议邀请发送、更新与取消

### 14.2 第二阶段

- 周期性会议增强
- 统计报表优化
- 管理员批量操作
- 同步补偿后台优化

### 14.3 第三阶段

- Outlook 插件
- 移动端 H5
- 电子门牌与签到联动

## 15. 风险与依赖

### 15.1 外部依赖

- 企业统一身份认证系统
- 企业员工通讯录
- IMAP Server
- SMTP 服务或企业邮件网关
- Outlook Classic Desktop App 客户端环境

### 15.2 主要风险

- 邮件网关对 `text/calendar` 或 `.ics` 的兼容性会影响邀请效果
- 不同 Outlook Classic 客户端版本对邀请渲染细节可能存在差异
- 参会人回复状态无法自动回写会影响高级协同体验
- 组织架构与员工通讯录数据不一致会影响参会人选择
- 历史会议室数据缺失会影响初始上线质量

## 16. 验收标准

- 管理员可成功完成会议室新增、编辑、停用
- 员工可按时间、地点、容量、设备完成会议室查询
- 空闲会议室可自动预订成功，冲突会议室不可重复预订
- 系统可正确展示会议室实时状态和未来日程
- 系统在冲突场景下可给出 Available 时间提示和替代会议室建议
- 预订成功后参会人可收到邮件通知
- 预订成功后，Outlook Classic Desktop 可识别会议邀请并生成日历事项
- 修改和取消预订后，Outlook Classic Desktop 可识别更新和取消邀请
- 管理后台可查看预订记录、通知状态和同步状态

## 17. 默认结论

本期产品定义为一套面向企业内部员工的 Web 会议室预订系统，采用“空闲即自动通过”的预订机制，并以企业 IMAP 邮件环境下的 Outlook Classic Desktop 会议邀请能力作为通知与日历协同方案。系统的核心价值在于提升会议室资源可见性、降低预订冲突、实现通知与日历邀请自动化，并为后续移动端、Outlook 插件和更深层日历平台集成打下基础。
